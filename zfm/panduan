 if [ "$test"x = "test"x ]; then                   #### 判断2个字符串是否一样   shell里面没有==的,那是c语言的等于

if (("$a" >= "$b"))   if [[ 3 -gt 2 && 2 -gt 3 ]] ##################### 数字比较

 if [[ "$a" > "$b" ]]  #############在ASCII字母顺序下比较

if [ $ANS="Yes" -o $ANS="yes" -o $ANS="y" -o $ANS="Y" ]  ############## 单括号 -o

if [[ "$user" != "root"  || （"$ip" != 192.168.1.9 && "$ip" != 192.168.1.200) ]]  ############双括号 ||
